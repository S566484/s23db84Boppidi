//- extends layout

//- block content
//-   h1= title
//-   p Welcome to #{title} I am Sowmya Reddy
//-   button(onclick="goFaster()") Click me for faster
//-   br 
//-   br
//-   div(id="faster") Increase the speed
//-   a(href="/Nuts") view nuts page

//-   script.
//-       var mph = 20;
//-       var change = 1;

//-       function goFaster() {
//-         mph += change;
//-         change += 1;
//-         document.getElementById("faster").innerHTML = `MPH ${mph} with change ${change}`;
//-       }
extends layout
block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/Nuts") Take me to nuts
    br
    a(href="/logout") Logout
